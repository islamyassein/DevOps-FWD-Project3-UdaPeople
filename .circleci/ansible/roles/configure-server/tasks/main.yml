---
- name: "update apt packages."
  become: true
  apt:
    update_cache: yes

- name: "upgrade packages"
  become: true
  apt:
    upgrade: yes

- name: "Add Node.js from Nodesource, and install NPM"
  become: true
  shell: |
    sudo apt-get install -y jq tar gzip git curl
    curl -fsSL https://deb.nodesource.com/setup_13.x | sudo -E bash -
    sudo apt-get install -y nodejs
    sudo apt-get install -y npm
    sudo apt-get update

# - name: "Add Node.js and NPM using ansible apt module"
#   become: true
#   apt:
#     name: ["nodejs","npm"]
#     state: latest
#     update_cache: yes

- name: "Install PM2 Globally"
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present